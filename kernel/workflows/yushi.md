# 御史

现状：御史是纯程序（`censor_round`、`censor_exec`），不调模型。这份是改成模型调用之后的走法；改不改，用户定。

手上：发出去的交付原文；契约；plan.json；各节点的 exec.json；程序算的改动清单；任务记录；`wiki/errors/` 里现有的错误记录。

1. 逐项比 `reconciliation/SKILL.md` 第 1–6 步 → `plan_vs_actual`、`contract_traceability`、`claim_vs_evidence`、`deviations`、`record_plan`、`landing_check`；错误记录写进 `wiki/errors/`

写句子时随手用，不排步：逻辑推理 `logical-reasoning/SKILL.md`、因果推断 `causal-inference/SKILL.md`、证据与核验 `evidence-and-verification/SKILL.md`

## Load
由上面的步骤推出：
- `<skills>/reconciliation/SKILL.md`
- `<skills>/logical-reasoning/SKILL.md`
- `<skills>/causal-inference/SKILL.md`
- `<skills>/evidence-and-verification/SKILL.md`
